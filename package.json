{
  "name": "amitygxmod-calculator",
  "version": "0.1.1",
  "scripts": {
    "clean": "rimraf dist/",
    "copy-files": "copyfiles -u 1 src/*.* dist/",
    "build": "tsc && sass src/scss/:dist/css/ && npm run copy-files"
  },
  "devDependencies": {
    "@types/copyfiles": "^2.4.4",
    "copyfiles": "^2.4.1",
    "rimraf": "^5.0.5",
    "typescript": "^4.5.4",
    "@types/chrome": "0.0.35"
  }
}
